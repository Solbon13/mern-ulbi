const defaulState = {
    currentUser: {},
    isAuth: false
}

export default function userReducer (state = defaulState, action) {
    switch (action.type){
        default:
            return state
    }
}